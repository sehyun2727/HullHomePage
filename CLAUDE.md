# HULL 홈페이지 리뉴얼 프로젝트 — 작업 브리프

> 이 문서를 세션 시작 시 반드시 먼저 읽을 것.
> 개별 태스크를 받더라도, 이 문서의 목표·원칙과 충돌하면 실행 전에 반드시 보고할 것.

---

## 1. 이 프로젝트가 뭔가

HULL株式会社(일본, 디지털사이네지·사인시공 회사)의 기업 홈페이지 `hull-inc.jp`를,
2026년 9월 시행되는 사내 조직개편에 맞춰 **정보구조(IA) 전면 재편**하는 프로젝트.

- **최우선 목표: 2026/8/20~8/25 사이에 메인서버(hull-inc.jp)에 반영, 8/26 공개**
- 담당자는 인턴 1명(성세현). 개발 지원 인력 없음.
- 상사(요시자와상)는 오픈 후 wp-admin에서 콘텐츠·CSS는 만질 수 있지만,
  DB 마이그레이션이나 서버 반영은 못 함 → **서버 반영은 담당자만 가능한 유일한 병목**

### 우선순위 (절대 뒤집히지 않음)

```
페이지 디테일 완성도  <  구조(IA) 확정  <  메인서버 반영 성공
```

- 리폼 페이지 내용, CSS 톤, 이미지 같은 건 **오픈 후에도 요시자와상이 고칠 수 있음** → 후순위
- 구조와 서버 반영은 담당자만 가능 → 최우선
- "완성도 높이려다 8/25를 놓치는 것"이 이 프로젝트 최악의 실패 시나리오

---

## 2. 환경 3개 — 각각의 역할

| 환경 | 위치 | 역할 |
|---|---|---|
| **hull-inc.jp** | 운영 서버 | 실제 라이브. **wp-admin 계정만 있음** (SSH/FTP 없음) |
| **hull-column-dev** | `C:\Users\sehyu\Local Sites\hull-column-dev` | 구버전 작업환경. 라이브 콘텐츠가 **일부만 잘려 들어온 불완전한 사본**. 이제 "이미 만들어둔 것의 참고 원본"으로만 사용. 여기서 신규 작업 금지 |
| **hull-homepage-renewal** | `C:\Users\sehyu\Local Sites\hull-homepage-renewal` | **현재 마스터 작업환경.** 2026/7/31 라이브 export를 그대로 import한 100% 미러. 앞으로 모든 신규 작업은 여기서 |

### 최종 반영 방식
hull-homepage-renewal을 완성 → 상사·사장 확인 → **이 환경을 통째로 hull-inc.jp에 반영**.
조각조각 옮기는 방식이 아님. 그래서 이 환경이 라이브의 정확한 파생본이어야 함.

### 백업 파일 신뢰도 경고
`hull-column-backup/`, `deployment-exports/` 안의 json/zip은 **전부 2026-07-14 시점 스냅샷**이다.
그 이후 손으로 반영한 작업이 다수 있으므로, **현재 상태 판단에 이 파일들을 근거로 쓰지 말 것.**
반드시 해당 사이트의 살아있는 DB를 WP-CLI로 직접 조회해서 확인할 것.
(과거에 이 백업만 보고 "메뉴가 구현 안 됐다"고 잘못 보고한 전례 있음)

---

## 3. 확정된 최종 사이트 구조 (기획서 v3, 변경 불가)

```
HULL
├── HULL VISION                    ← 디지털사이네지시공부 (내부에 디지털사이네지,도코데모사이네지,탑보드사이네지로 넘어갈수있도록 할거임)
│
├── HULL SPACE                     ← 공간연출사인시공부 + 신설 리폼사업부(내장/외장/설비까지 확장)
│   ├── サイン事業                 → 기존 페이지 재활용 (slug: sign, 구 표시명 "サイン施工・看板製作"/"サイン")
│   └── 空間施工事業               → 신설 (slug: reform-business, 구 표시명 "リフォーム")
│       ※ 2026-08-17 대표 명칭 확정: サイン事業 / 空間施工事業 (URL·slug는 유지, 표시명만 변경)
│
├── トピックス
│   ├── ニュース                   → 기존 뉴스 카테고리 아카이브
│   └── コラム                     → 컬럼 CPT 아카이브
│
├── 会社情報
│   ├── 企業概要                   → 기존 会社概要 + 沿革 통합
│   └── 企業理念                   → 기존 経営理念 + メッセージ 통합
│
└── お問い合わせ                    → 기존 폼 재활용
```

### 이 구조에서 명시적으로 제외된 것
- **輸入雑貨販売・EC事業** — 메뉴에서 완전 삭제 결정됨. 남아있으면 안 됨
- **実績紹介** — 초기안에 있었으나 サイン/リフォーム 체제로 대체되어 폐기됨
- **Q&A** — 이번 스코프에서 제외 (제품 페이지 내 FAQ 섹션과는 별개 사안)

### 헷갈리기 쉬운 함정 2가지
1. **리폼이 두 개 있다.** 컬럼 카테고리 `reform`(콜럼 기사 5건 묶음)과, HULL SPACE 하위 `リフォーム`(신설 사업부 소개 고정페이지, slug: `reform-business`)는 **완전히 다른 것**. 메뉴의 リフォーム를 컬럼 카테고리로 연결하면 안 됨.
2. **기존 메뉴 라벨을 재활용하는 게 아니라 IA 전체를 새로 짜는 것.** 기존 항목에 새 항목을 덧붙이는 방식이면 틀렸음. 위 구조가 **최종 상태 전부**이며, 여기 없는 항목은 메뉴에 존재하면 안 됨.

---

## 4. 공개 URL 정책 (중요)

**기존에 공개돼 있던 URL은 임의로 바꾸지 말 것.** 바꿔야 할 이유가 생기면 반드시 먼저 보고.

- 컬럼 아카이브는 `/column/` 이다. 프리즈된 매뉴얼(한/일), SEO 전략 문서가 전부 이 URL 기준으로 작성돼 있음
- 카테고리별: `/column/category/{slug}/`, 태그별: `/column/tag/{slug}/`
- hull-homepage-renewal은 라이브와 동일한 퍼머링크 구조(`/archives/...` 계열)를 쓰므로, CPT/택소노미 rewrite에 front가 자동으로 붙어 `/archives/column/` 이 되는 현상이 발생할 수 있음
- **이때 올바른 대응은 rewrite에 `with_front => false`를 지정해 `/column/`을 유지하는 것.** 사이트에 맞춰 공개 URL을 바꾸는 방향은 틀림
- 라이브 퍼머링크 설정(category_base 등)은 라이브가 기준. 로컬을 라이브에 맞출 것

---

## 5. 진행 상태

### 완료
- hull-homepage-renewal에 2026/7/31 라이브 export import 완료 (100% 미러 확보)
- 컬럼 CPT / 택소노미 / 게시글 20건 / 썸네일 이관 완료
- lightning-child 자식테마 이관 완료
- 메뉴 IA 전체 재구성 (mainMenu를 3절 확정 구조와 일치하는 11개 항목으로 재구축, 실제 렌더링·URL 200 확인 완료)
- HULL VISION: 드롭다운 없이 `/digital-signage/`(기존 페이지) 단일 링크로 연결 완료. 페이지 내부 사이네지 서브 항목(도코데모→탑보드 등 수정)은 **의도적으로 스코프 밖, 추후 별도 작업**
- HULL SPACE 하위 서브메뉴 2개(サイン/リフォーム) + `reform-business` 페이지 생성 완료
- 컬럼 공개 URL `/column/` 복원 완료 (with_front=false 적용, `/archives/column/` 회귀 해결)
- 会社情報 → 企業概要/企業理念 2페이지 통합 **실행 완료** (2026-07-31 사용자 명시 지시로 실행): 企業理念(3080) 안의 沿革 섹션을 会社概要(2961) 최하단으로 이동, 그 자리에 메시지 페이지(3077) 콘텐츠를 이식. `/philosophy/`·`/company/` 렌더링 결과로 검증 완료
- 상위 메뉴 4개("#" 플레이스홀더)에 실제 링크 연결 완료: HULL VISION→`/digital-signage/`, HULL SPACE→`/sign/`, トピックス→`/archives/category/news/`, 会社情報→`/company/`
- HULL SPACE 하위 대표 명칭 리네이밍 완료 (2026-08-17): 서브페이지 post_title/H1, HULL SPACE(7005) Hero H1·사업카드 2개 제목, 헤더 mainMenu 서브메뉴 2개, AIOSEO title/description(7005/3094/6834)을 `サイン事業`/`空間施工事業`로 통일. URL(`/sign`, `/reform-business`)은 변경 없음. 세부 설명 문구 안의 `サイン施工・看板製作`/`リフォーム` 표현은 의도적으로 유지(허용 범위)
  - **2026-08-24 명칭 추가 확정**: 가이드라인 이미지 우선 원칙에 따라 3094/6834 hero の JP テキスト(h1)는 `サイン施工`/`空間施工`を使用(post_title·메뉴 라벨의 `サイン事業`/`空間施工事業`와는 별도 — hero 캐치카피로서의 표기). page_title/AIOSEO/메뉴는 변경 없음.
- Footer(footer-widget-3, widget_block id 16) 기존 버그 수정: "HULL SPACE" 항목이 `/sign/`으로 링크되고 JP라벨이 `空間施工事業`로 잘못 표시되던 것을 `/space/` 링크 + JP라벨 삭제로 수정
- **HULL SPACE 부모 랜딩페이지(ID 7005, `/space`) 비주얼 리뉴얼 완료 (2026-08-17)** — 스코프는 이 페이지 하나만(`/sign`, `/reform-business` 내부·TOP 페이지는 미포함). 이미지 생성 툴이 없어 미디어 라이브러리에 있던 미사용 실제 HULL 사진으로 대체(신규 생성 아님):
  - Hero: `min-height` 60vh(가변) → 400px(데스크톱)/모바일 460px 고정값으로 변경. 이미지·카피는 기존 유지
  - About: 다른 페이지(VISION)에서 빌려온 `placeholder-vision-about.png` 플레이스홀더 이미지를 실제 HULL SPACE 브랜드 사진(id 6918)으로 교체, 그레이스케일 은폐 필터 제거. TOP의 `.section-jp-label`(32px/700 JP 주제목) + `.section-en-subtitle`(16px 보조제목) 제목 위계 패턴을 이식해 JP 대제목 "空間をつくり、支える。" 신규 추가
  - 実績: 콘텐츠·이미지 그대로 유지, 원형 그리드만 160px→180px로 확대, 라벨을 section-jp-label 스타일로 통일(h2 문장+숫자는 문장형이라 24px 유지, 근거는 CSS 주석에 기록)
  - 2つの事業/HULL SPACEの強み/施工事例: 라벨-제목 위계 동일 패턴으로 전체 통일
  - 2つの事業 카드: flex+stretch로 두 카드 높이·버튼 위치 정렬 통일
  - HULL SPACEの強み: 기존 `#dcdcdc` 회색 경계선(hairline) 구분을 제거하고 TOP 강점 섹션의 "border 대신 whitespace" 원칙 이식 + 브랜드 그린(#49a339) 세로 악센트 바 추가
  - 施工事例(Works): 문자 그대로 "画像"/"事例タイトル" 텍스트 플레이스홀더였던 카드 6개 → 실제 HULL 시공 사진 5장(기존 미디어 라이브러리, 다른 페이지에서도 쓰이는 것 재활용, 신규 촬영/생성 아님)으로 교체. 카드 제목/설명은 특정 사례를 확정 실적처럼 과장하지 않고 범용적 공정 표현으로 작성(サイン設置工事/施工現場での取付作業/現場での施工管理/ご相談・ヒアリング/企画・デザイン検討)
  - CTA: padding 120px → TOP과 동일한 140px로 통일 (버튼/폰트 크기는 이미 일치했음)
  - Footer: 기존 a안(HULL SPACE 단일 라인, `/space/`, 하위 항목 없음) 유지 확정 — 변경 없음
  - CSS는 전부 `body.page-id-7005` 스코프([SPACE] 블록, Additional CSS post 2641)에만 추가/수정, 다른 페이지에 영향 없음
  - 반응형 375/768/1024/1440 확인: 기존 사이트의 600px/781px/782px 브레이크포인트 컨벤션 그대로 사용, 내부 링크 전체 200 확인, 남은 플레이스홀더 자산 없음

### 미완료 · 요확인
- TOP 페이지(ID 2659, `/`)의 "About HULL" 카드에서 HULL Space 카드가 제목 `空間施工事業`인 채로 링크는 `/sign`(サイン事業 페이지)을 가리킴 — footer와 동일한 종류의 오류지만 TOP 페이지는 HULL SPACE 담당 스코프 밖이라 미수정, 발견만 기록 (2026-08-17)
- 레거시 페이지 정리 (`旧会社概要`(slug: about, private), `/services_old`, `/signdisplay` 등) + 301 리다이렉트
- 메시지 페이지(`/message/`, post ID 3077): 内容을 企業理念(3080)으로 옮긴 후에도 페이지 자체는 publish 상태로 남아 있음(중복 콘텐츠, 메뉴에서는 미노출). 삭제/비공개화/리다이렉트 여부 미결정 — 파괴적 작업이라 사용자 승인 필요
- HULL VISION 랜딩페이지(`/digital-signage/`) 내부 사이네지 링크를 도코데모사이네지/탑보드사이네지 구조로 수정 — 사용자가 "나중에"로 명시, 현재 스코프 아님

### 이번 스코프 밖 (지금 건드리지 말 것)
- 디자인/CSS 톤 조정 — **HULL SPACE 부모 페이지(`/space`, ID 7005)는 2026-08-17 완료**. `/sign`(3094)·`/reform-business`(6834) 내부 콘텐츠는 **2026-08-24 T3 지시서 v2로 스코프 IN됨** (Step B~E 실행 중). TOP 페이지 디자인은 여전히 스코프 밖.
- `.page-header` 배경색 이슈 — 이미 조치됨, 재론 불필요
- 리폼 페이지 실제 콘텐츠(PDF 기반 레이아웃) — 오픈 후 작업 가능

---

## 6. 작업 원칙 (위반 시 즉시 중단하고 보고)

1. **방법을 지정받았으면 그 방법으로 한다.** 막히거나 더 나은/위험한 대안이 보여도 **먼저 보고하고 승인받은 뒤** 변경할 것. 특히 스스로 "리스크 있다 / 검증 안 됐다"고 표현한 방법은 명시적 승인 없이 절대 먼저 시도하지 말 것. (과거 위반 전례 있음)

2. **"조사만" 지시받으면 실행하지 않는다.** 콘텐츠 병합처럼 판단이 필요한 작업은 조사·제안까지만 하고 대기.

3. **현재 상태는 항상 살아있는 DB로 확인한다.** 백업 json/zip은 구버전이므로 근거로 쓰지 않는다.

4. **환경별 고유 설정을 확인한다.** 다른 사이트의 설정 파일(wpcli-php.ini 등)을 복사 재사용할 때 MySQL 포트·DB명 등 사이트별 값이 섞이지 않았는지 반드시 검증. (과거에 잘못된 포트로 다른 사이트 DB에 연결된 전례 있음)

5. **파괴적 작업 전에는 보고한다.** 삭제, 덮어쓰기, 대량 업데이트, DB 스키마 변경.

6. **보고는 "무엇을 했는가"가 아니라 "확정 구조 대비 지금 어떤 상태인가"로 한다.** 각 항목을 3절의 확정 구조와 대조해 완료/부분/미완으로 표시하고, 실제 URL 접근 결과로 검증한 내용을 포함할 것.

## 7. 콘텐츠 취급 원칙

- **페이지를 삭제하지 않는다.** 정리가 필요하면 비공개(private) 전환만 한다.
  휴지통 이동, 완전 삭제, DB 행 제거 전부 금지.
- 언제든 되살릴 수 있는 상태를 유지한다. 상태 변경 전 원래 값을 기록한다.
- 콘텐츠를 다른 페이지로 이관한 뒤 원본을 정리할 때는,
  이관이 실제로 완료됐는지 먼저 검증하고 나서 처리한다.